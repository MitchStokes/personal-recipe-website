{
  "name": "recipe-lambda-functions",
  "version": "1.0.0",
  "description": "Lambda functions for recipe website",
  "main": "index.js",
  "scripts": {
    "build": "zip -r function.zip ."
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.0.0",
    "@aws-sdk/lib-dynamodb": "^3.0.0",
    "uuid": "^9.0.0"
  }
}